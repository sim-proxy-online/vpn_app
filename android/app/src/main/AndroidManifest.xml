<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Отключение оптимизации батареи (Doze), чтобы VPN не убивался в фоне. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
    <!-- Самообновление: скачать APK и запустить его установку из приложения. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- Android TV / планшеты: приложение работает без сенсорного экрана и
         появляется в лаунчере Android TV (leanback). Обе фичи НЕ обязательны,
         чтобы не отсекать обычные телефоны. -->
    <uses-feature android:name="android.software.leanback" android:required="false" />
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />

    <application
        android:name=".SimApplication"
        android:label="Sim Proxy"
        android:icon="@mipmap/ic_launcher"
        android:banner="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true"
        android:hardwareAccelerated="true">

        <!-- Disable Sentry Auto-Init to prevent crash on startup -->
        <meta-data android:name="io.sentry.auto-init" android:value="false" />
        <meta-data android:name="io.sentry.enabled" android:value="false" />
        <meta-data android:name="io.sentry.dsn" android:value="" />

        <provider
            android:name="io.sentry.android.core.SentryInitProvider"
            android:authorities="com.simproxy.app.SentryInitProvider"
            android:exported="false"
            tools:node="remove"
            xmlns:tools="http://schemas.android.com/tools" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@android:style/Theme.NoTitleBar"
            android:launchMode="singleTask"
            android:configChanges="orientation|keyboardHidden|screenSize|smallestScreenSize|screenLayout|density|uiMode">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <!-- Появление в лаунчере Android TV -->
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
            <!-- Deep links: открытие ссылок sim:// и happ:// из браузера/других
                 приложений (импорт подписки, сервера, профиля маршрутизации). -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="sim" />
                <data android:scheme="happ" />
            </intent-filter>
            <meta-data android:name="android.app.shortcuts" android:resource="@xml/shortcuts" />
        </activity>

        <service
            android:name=".VpnServiceImpl"
            android:permission="android.permission.BIND_VPN_SERVICE"
            android:foregroundServiceType="specialUse"
            android:exported="false">
            <intent-filter>
                <action android:name="android.net.VpnService" />
            </intent-filter>
            <property android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                      android:value="VPN tunnel"/>
        </service>

        <!-- AntiFilterService — лёгкий фоновый сервис для поддержания SOCKS-соединения
             через прокси при выключенном основном VPN. Захватывает живое TCP-соединение,
             отправляет SOCKS-пинги каждые 45 сек для предотвращения закрытия ISP'ом. -->
        <service
            android:name=".AntiFilterService"
            android:foregroundServiceType="specialUse"
            android:exported="false">
            <property android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                      android:value="SOCKS keep-alive service" />
        </service>

        <receiver android:name=".NetworkStateReceiver" android:exported="false">
            <intent-filter><action android:name="android.net.conn.CONNECTIVITY_CHANGE" /></intent-filter>
        </receiver>

        <receiver android:name=".BootReceiver" android:exported="false">
            <intent-filter><action android:name="android.intent.action.BOOT_COMPLETED" /></intent-filter>
        </receiver>

        <receiver android:name=".WatchdogReceiver" android:exported="false">
            <intent-filter><action android:name="com.simproxy.app.WATCHDOG_CHECK" /></intent-filter>
        </receiver>

        <!-- FileProvider — отдаёт скачанный APK обновления системному установщику
             по content:// URI (требование Android 7+; file:// больше нельзя). -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.simproxy.app.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- QrScannerActivity наследует AppCompatActivity → ей ОБЯЗАТЕЛЬНА
             AppCompat-тема, иначе активность падает при старте (краш сканера). -->
        <activity android:name=".QrScannerActivity"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
    </application>
</manifest>
